This is a small chatroom app built with nodejs and socket.io
"npm install" in your terminal to install all dependencies
To run the chatapp run "node server.js" in your terminal in the parent directory